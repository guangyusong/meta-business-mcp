# Local stdio example

This example runs the skeleton MCP server locally.

```sh
npm install
npm --workspace @meta-business-mcp/server run dev:stdio
```

No Meta credential is required. Tool calls return safe skeleton responses until live handlers are intentionally added.
